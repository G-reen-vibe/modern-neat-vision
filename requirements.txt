# CPU-only PyTorch wheels are pulled from the CPU index URL:
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cpu

torch>=2.0
torchvision>=0.15
timm>=1.0
datasets>=2.0       # HuggingFace datasets — used as CIFAR backend (cs.toronto.edu mirror is slow)
huggingface_hub>=0.20
networkx>=3.0
numpy>=1.24
scipy>=1.10
pandas>=2.0
pyyaml>=6.0
tqdm>=4.65
thop>=0.1
matplotlib>=3.7
